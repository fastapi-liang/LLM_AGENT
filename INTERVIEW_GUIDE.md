# LX-AICODING 面试介绍指南

> **这份文档不是讲项目是什么，而是教你怎么"讲"这个项目。**
> 面试的关键不是技术名词，而是 **"为什么这样设计"**。
>
> 📚 配套文档：
> | 文档 | 内容 |
> |---|---|
> | [PROJECT_ANALYSIS.md](./PROJECT_ANALYSIS.md) | 项目理解（是什么 / 怎么组织） |
> | [SYSTEM_ARCHITECTURE.md](./SYSTEM_ARCHITECTURE.md) | 系统架构（架构图 / 时序图 / 数据库关系） |
> | [ONBOARDING_GUIDE.md](./ONBOARDING_GUIDE.md) | 新人上手指南（从哪里看 / 怎么看） |
> | **本文件** | **面试话术（怎么介绍 / 怎么应对追问）** |

---

## 目录

1. [介绍策略总纲](#1-介绍策略总纲)
2. [30 秒版：电梯演讲](#2-30-秒版电梯演讲)
3. [3 分钟版：完整叙事](#3-3-分钟版完整叙事)
4. [技术点速查卡：一句话怎么说](#4-技术点速查卡一句话怎么说)
5. [高频追问与应答](#5-高频追问与应答)
6. [三个必须避免的坑](#6-三个必须避免的坑)
7. [面试自测清单](#7-面试自测清单)

---

## 1. 介绍策略总纲

核心就一句话：**用"解决什么问题"来串联技术点，而不是报菜名式地罗列技术栈。**

| 原则 | 做法 | 反例 |
|---|---|---|
| 每个亮点必须回答"为什么" | 讲安全 = 讲"Agent 真实操作机器，权限越大风险越大" | "我用了白名单、脱敏、SSRF 防护"（没有为什么） |
| 主动讲取舍和失败 | "我把三套数据拆开避免双源打架" | 只讲成功 |
| 诚实定位 | "这是教学项目，我刻意做了功能减法" | 包装成大规模生产系统 |
| 留钩子 | 30 秒讲完抛一句"想不想听我最难的一个问题" | 一口气把所有细节倒完 |

**叙事公式**：`背景/动机 → 我的角色 → 三个亮点（每个带 why）→ 一个难点（展示解决过程）→ 收获`

---

## 2. 30 秒版：电梯演讲

> "我参与了一个 **AI Coding Agent 教学平台**的后端。它让大模型 DeepSeek 扮演真正的程序员：用户在网页上给一个 Gitee 仓库地址和一句需求，Agent 会真实地去克隆代码、读源码、改代码、跑测试、提交推送，最后创建 Pull Request。整个后端是我负责设计实现的，核心是 **FastAPI + DeepAgents（LangGraph）+ 三套 SQLite** 的架构。"

- 一句话说清"做了什么"；
- 抛技术栈；
- 最后加一句钩子：**"这个项目最特别的，是让 AI 真实操作电脑时怎么保证安全——你想听听我怎么做安全设计的吗？"**

---

## 3. 3 分钟版：完整叙事

### 背景（10 秒）
> "这是一个教学场景：让学生看到 AI Agent 是怎么搭起来的，所以核心诉求是**架构清晰 + 安全可控**。"

### 亮点 1：让 AI 真实操作电脑，安全是地基
> "Agent 要真实执行命令、改真实仓库，所以我把安全做成了**系统能力**而不是碰运气，做了六层防护：
> - **Token 脱敏**：Gitee Token 全链路替换成 `***`，Git 认证用 askpass 环境变量注入，Token 永远不进命令字符串、不进日志、不进模型上下文；
> - **命令白名单**：Agent 只能执行 `git/python/pytest` 这类白名单命令，拒绝 shell 操作符和危险命令；
> - **路径边界**：所有文件操作必须落在工作区内，拒绝 `..` 穿越；
> - **只读约束**：`skills / policies / .secrets` 等关键目录只读；
> - **SSRF 防护**：抓 URL 时做 DNS pin + 内网 IP 黑名单；
> - **运行限额**：限制工具调用次数和时长，防止模型死循环。
>
> 为什么这么设计？因为 **Agent 的权限越大风险越大，防护必须在框架层做死，不能依赖模型自觉。**"

### 亮点 2："先方案、后实施"的人机协同
> "最得意的一个设计是**人在回路**。默认情况下，Agent 不会一上来就改代码：用户提需求 → 强制进入 planning 只出技术方案 → **用户确认后**才进入 coding。
> 这里的工程难点是：用户回复'确认实施'时，runtime 不能把'确认'两个字当新需求，必须回到 checkpoint 存档里**反查最近一份待确认的方案**，用里面保存的原始需求（`source_prompt`）作为执行目标。这样保证了 **Agent 永远在人类确认后才动代码**，多轮修改方案也不会丢失原始需求。"

### 亮点 3：双数据源的存储设计
> "我把数据拆成三套 SQLite，职责完全分开：
> - **checkpoints.sqlite**：聊天历史（LangGraph checkpoint 管理，是前端历史的**唯一来源**）；
> - **store.sqlite**：业务台账（会话列表、运行状态、审查发现）；
> - **langgraph_store.sqlite**：仓库级长期记忆（每个仓库一份 `/memories/{owner}/{repo}.md`，任务结束自动提炼技术栈、测试命令）。
>
> 关键取舍是：**聊天正文只从 checkpoint 读，绝不从 Store 读**，从根上避免了'两个数据源打架'（重复、乱序、覆盖）的问题。"

### 一个难点（展示解决问题的过程）
> "最头疼的是**意图分类的可靠性**——因为 coding 是唯一允许改代码的模式，模型判断错了后果严重。我的方案是三层保障：低成本任务用关键词直接判断（省模型调用）→ 复杂任务用模型结构化输出 → **任何结果都要过一道安全规则**，比如用户说了'只分析'，即使模型判成 coding 也会被强制降级为只读。模型不可用还有关键词兜底，保证系统永远可降级。"

---

## 4. 技术点速查卡：一句话怎么说

| 技术点 | 一句话怎么说 | 面试官潜台词 |
|---|---|---|
| 安全六层防护 | "防护做在框架层，不依赖模型自觉" | 你有没有安全工程思维 |
| 先方案后实施 | "用确定性规则保证人类确认后才动代码" | 产品思维 / 流程控制 |
| 三套 SQLite | "聊天历史、业务台账、长期记忆职责分离" | 数据建模判断力 |
| 聊天正文只读 checkpoint | "避免双数据源冲突" | 架构取舍意识 |
| 意图分类三层保障 | "权限边界必须确定性兜底，模型只能提升泛化" | 容错 / 降级设计 |
| SSE 实时流 | "后台线程跑 Agent，asyncio 队列桥接回事件循环" | 并发 / 异步理解 |
| 仓库级长期记忆 | "任务结束自动提炼，下次处理该仓库有'小抄'" | 工程闭环思维 |
| 子 Agent 权限收敛 | "审查子 Agent 只读，不能改代码" | 权限最小化 |

---

## 5. 高频追问与应答

### Q1："为什么用 DeepAgents 而不自己写 Agent？"
- **回答思路**：教学项目，复用框架成熟能力是合理的；但强调**安全、流程、数据设计是自己的**。
- **话术**："框架负责 Agent 编排、子 Agent、checkpointer 这些通用能力；但安全边界、任务流程、数据架构是我自己设计的。这正好体现我**知道哪些该用框架、哪些该自己造**。"

### Q2："checkpoint 和 store 为什么分开？"
- **回答思路**：对话要恢复上下文，台账要查询展示，生命周期和读写模式不同。
- **话术**："聊天历史是'对话'，要恢复 Agent 的上下文；业务数据是'台账'，要供前端查询。两者生命周期不同，分开后前端展示完全不依赖 Agent 内部状态。"

### Q3："怎么防止 Agent 乱执行命令？"
- **回答思路**：三层递进——命令白名单、参数清洗中间件、工具层拒绝。
- **话术**："三层：一是 `permissions.py` 的命令白名单，只放行 git/python 等；二是 `SanitizeToolInputsMiddleware` 在执行前清洗路径和参数；三是工具层，比如只读任务里'创建 PR'的工具会被直接拒绝。"

### Q4："Token 怎么保证不泄露？"
- **回答思路**：统一脱敏入口 + 认证不进命令。
- **话术**："全项目只有一个 `mask_token()` 权威实现，命令输出、日志、异常、模型上下文全部过一遍；Git 认证用 askpass 脚本 + 环境变量注入，Token 从不拼进命令字符串。"

### Q5："意图分类为什么不用纯模型？"
- **回答思路**：权限边界要确定性兜底。
- **话术**："因为 coding 是唯一允许写代码的模式，如果完全交给模型判断，一旦误判就改坏了代码。模型负责泛化，后端规则负责兜底——安全决策永远不能外包给模型。"

### Q6："如果让你加一个新任务类型？"
- **回答思路**：证明你知道扩展点（4 步）。
- **话术**："四步：一是在 `task_intent.py` 的 `TaskKind` 加枚举；二是加关键词兜底规则；三是在 `prompt.py` 加对应的系统提示词；四是在 `runtime.py` 判断是否要加流程分支。改完还要确认只读权限是否符合预期。"

### Q7："这个项目有什么缺点？"
- **回答思路**：主动暴露缺点，比被问出来加分。
- **话术**："三个：一、路径和工作区硬编码 Windows（`E:\ai_workspace`），跨平台要适配；二、有些遗留死代码和旧表结构（`momory_update.py`、`repo_mapping.py`）没清理；三、只接了 DeepSeek 单模型，没有做多模型 fallback——这些是我后续想改进的方向。"

### Q8："遇到过最难的 bug？"
- **回答思路**：讲一个具体、有过程的问题。
- **话术**："Windows 上 git pull 偶尔报 `FETCH_HEAD` 权限错误——是杀毒软件或 IDE 短暂占用临时文件导致的。我加了探测逻辑：检测到该错误就删除 FETCH_HEAD 后重试，并在外层做 token 脱敏。这让我学会了'在异常路径上也要考虑兼容真实环境'。"

---

## 6. 三个必须避免的坑

1. **别把教学项目包装成生产系统**——诚实说"我刻意做了功能减法"，反而显得有判断力。
2. **别只报技术名词**——每个技术点都跟上"为什么"，面试官一听就知道你真做过还是背的。
3. **别跳过失败和取舍**——主动讲踩过的坑（FETCH_HEAD、意图误判风险），比只讲成功更有说服力。

---

## 7. 面试自测清单

对以下每条打 ✓，练到能脱稿自然说出来：

- [ ] 30 秒版：能在一分钟内说完"做了什么 + 技术栈"，并抛出钩子
- [ ] 3 分钟版：能讲清"背景 → 三个亮点（每个带 why）→ 一个难点"
- [ ] 安全：能一口气说出六层防护，并解释为什么防护要在框架层
- [ ] 人机流程：能解释"确认实施"时如何从 checkpoint 反查方案还原需求
- [ ] 数据：能说清三套 SQLite 的分工，以及为什么正文只从 checkpoint 读
- [ ] 追问：对上面 8 个高频问题都能给出"思路 + 话术"
- [ ] 缺点：能主动、自然地暴露 2-3 个项目不足
- [ ] 对答中时刻记得：每个技术点都带上"为什么"

---

*本文件为面试话术指南，配合 `PROJECT_ANALYSIS.md`、`SYSTEM_ARCHITECTURE.md`、`ONBOARDING_GUIDE.md` 使用。*
